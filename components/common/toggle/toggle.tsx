import * as React from 'react'

export const ToggleOption: React.FC<ToggleProps> = ({

}): React.ReactElement => {
    return(
        <div></div>
    )
}


export type ToggleProps = {
    toggleStatusIdentifier: number
}
export type AssignmentPropType = {
	type: string
	average: number
}